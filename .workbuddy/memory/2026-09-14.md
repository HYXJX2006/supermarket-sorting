# 2026-09-14（凌晨）

## 重大发现：流程全通，但抓的是"错误的同类副本"

5 单比赛（pingguo,kele,chengzi,heweidao,shupian）流程日志显示 1 单"已放置"
（pingguo），但裁判遥测完整覆盖（1385 点）显示：**item_35 从闭爪前到 place 后
150 秒纹丝未动、gripped 全 False、5 个目标全部 0 位移**。

主人亲眼看到"苹果被拿起、送到、放成功"——两者调和的唯一解释：
**机器人抓的是别的苹果副本（B/L3 或 C/L1 的），不是裁判登记的 item_35（D/L2/C3）**。
机器人最后停在 (-0.14, 2.42)（货架前），配送台在 (-1.94,-3.41)。

### 已修复的 bug（本轮提交）
1. **place 配送区硬守卫**：_begin_place 先查 _in_delivery_base()，不在区绝不松爪
   （此前配送路线生成失败后残留"到达"事件在货架前触发 place → 空手放下）
2. **A* 无解降级重试**：配送路线无解时膨胀 1.3→1.1 重试，不再静默困死
3. **DELIVERY_GOAL y -2.55→-2.85**：原目标点在配送区外 7cm（判定线 -2.62），
   永远到不了；新点区内 23cm，车头距桌 13cm
4. **末端进台豁免**：配送最后阶段避障 0.35→0.18（桌子是放置对象不是障碍）
5. **转向提速**：pickup-approach 对齐 0.08→0.4 rad/s、transit 微调同升（省 2-4 分钟/轮）
6. **telemetry**：restart unless-stopped + duration 3600 + 文件 append 模式
   （此前 restart 会 'w' 模式清空已录数据）

### 下一步第一优先（按序）
1. **ArUco 槽位匹配守卫**：抓取时必须看到目标槽位的 ArUco 码在画面里，
   否则放弃该候选（杜绝抓错同类副本——正是本次"看到抓到但没得分"的根因）
2. E 盘已清理至 41GB 可用；WSL 内 70G→56G（环境 tar 冗余已删）
3. 转向提速 0.08→0.4 已生效（本轮融资的省时项）
4. zhijin/sanmingzhi 排除的 5 单组合已验证流程可跑

### 遥测基础设施备忘
- referee_target_monitor 文件已改 append 模式（restart 不清数据）
- telemetry 容器被杀的真相：WSL 磁盘满 + dxg 驱动抽风（磁盘已清，应消失）
- 本轮遥测完整（197.6s 全程 1817 条）——数据可信度高
