
## 12:15 按主人指令：工作区完全回归 ZCode 的 origin/develop（9226489）

**操作**：`git restore --worktree -- baseline/`（回到 HEAD = 9226489，09-12 23:48
"close the vertical loop"）+ 容器同步 + 清 pycache。工作区干净（只剩 memory
与未跟踪的诊断工具/答辩文档）。

**随回归一起消失的我这边改动**（主人知悉）：
- A* 膨胀 1.7（配送走中间）—— NAV_INFLATE
- 伺服首帧中心半径防线（锁错红柱问题回到无防线状态）
- 垂直抬高 2cm（dz +0.010/+0.050）
- 伺服垂直闭环禁用（SUPERMARKET_SERVO_HEIGHT_LOOP）
- tuck_arm/carry_pose/深度探测等

**保留**：答辩文档（docs/作品技术方案与测试报告.md）、诊断工具
（diag_delivery_freeze.py / pose_test.py / parse_layout.py / test_aruco_avail.py）。

**注**：主人之前确认过的三个固有问题的修复都随回归消失（锁错目标、z 层级
37cm、配送贴墙）—— 这些是 ZCode 版本固有的，回归后若复现属预期。

**git restore 后 CRLF 检查**：本次 restore 未产生 CRLF（转 LF 脚本报 0 个文件
需要修），文件行尾正常，脚本可直接运行。

�用（glfw/EGL 双断），跑仿真必须先起 X
4. run_random5 脚本：伺服调试/偏置环境变量透传已加
   （SERVO_DEBUG / SERVO_AIM_OFFSET_V / SHUPIAN_SERVO_AIM_OFFSET_V / MARKER_ROI_*）

**明早第一步**：对着窗口核 vertical_err 的符号语义 + aim_offset 方向，
把 6.7cm 残差收敛进 15mm 门禁，薯片即可走完"抓取→配送"全链。


---

# 09-13 晚（ZCode 会话）：配送卡墙三件套 + 里程碑

## 🏆 里程碑：比赛版"抓取→配送→放置→返程"首次完整走通
（17:04 轮：吸附→FK伺服 err 0.1cm→闭爪 0.772→抬升→配送卡死→倒车脱困→放置→返程）

## 配送卡墙三件套（主人报告"左墙右障手臂卡住"后落地）
1. **障碍生成靠墙**：obstacle_layout.py 采样后向就近 x 侧墙推 0.15m
   （温和版；0.30/交替贴墙被校验器拒绝——5000 次全败，拒绝原因：
   起点终点被堵/不强制绕行）。7/7 种子通过全部校验
2. **server 挂载同一份生成器**（run 脚本新 volume），场景与 A* 复现一致
3. **膨胀回 1.35**：靠墙障碍已清空中央，1.7 反而让 A* 无路（实测路线生成失败）

## ArUco 吸附终于真正生效（三层修复）
1. 反向映射 ARUCO_ID_BY_LABEL：标签→码号（**原来根本没人给 cand.aruco_id
   赋值**，吸附永远静默跳过——这是个会话间的断线）
2. 可见性闩锁：检测器频繁发空帧，改按 ID 记最近可见时间
3. 标签锚定：名义坐标来自货位标签+固定几何，**不要求见到码**（检测器
   稀疏，目标槽位的码可能整轮检不到）；8cm 容差守卫保留
实测：`ArUco 吸附：B/L2/C3 (码 14) x/z -0.640/0.915 → -0.630/0.910`

## 世界系 FK 伺服（主人方案，已实现）
- error = 目标x − FK(实测关节, odom)，Δj3 = err/数值FK灵敏度
- 实测收敛到 **err 0.1cm**（吸附正确时）；手眼图像完全退出控制回路
- 手眼图仅诊断用途

## x 偏置链清理（主人定案）
- DEPLOY_DX = +0.02（主人定）、ARM_LATERAL_OFFSET_M = 0、shupian dx = 0
- 教训：三层手动偏置互相打架（-0.011 手动 + -0.060 lateral + 伺服），
  永不收敛；**归零 + 实测闭环才是正解**

## 当前状态（本轮 17:29 结束）
- 本轮抓取失败（feedback 0.156/0.101 擦边）——上轮同配置抓取成功（0.772）
  差异：本轮 DX=+0.02 + 停止线 -0.010。**待排查**：DX=+0.02 是否过偏
  （伺服数据在 servo_result.json）、-0.010 停止线是否过深
- 配送超时仍在（本轮卡死→倒车→推进了但没跑完 150s；RTF 0.13-0.20 环境负载高）
- 计分认知：裁判按指派 body 计分（抓同类副本不计分，见 referee.py
  remaining/scored 逻辑）——5 单正式赛需"命中指派副本"，副本不可视认，
  需"计分验证重试"机制或确认正式裁判按品类计分（README 说任意有效搬运得分）
- 遥测容器 2 分钟早死（Exited 1），最终得分没录到，待修

## 待办
- [ ] 排查 DX=+0.02 vs 停止线 -0.010 的最优组合（servo_result.json 的
  iterations 有每轮 ee/err 数据）
- [ ] 修遥测容器早死
- [ ] 裁判计分判定：跑 TASK_COUNT=1 看"抓副本"是否得分（server 日志/referee）
- [ ] 9 类批量（run_kind_tests.sh）
- [ ] 全部提交（本轮未提交：归零偏置、DX+0.02、深度-0.010、墙靠生成器）
