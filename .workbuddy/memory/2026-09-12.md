
## 22:30 输出《作品技术方案与测试报告》（供答辩材料编写）

**文件**：`docs/作品技术方案与测试报告.md`（513 行 / 约 1.7 万字符）

**内容结构**（7 章 + 代码逻辑详解）：
1. 项目概述（赛题、任务定义、S1–S5/B1 计分、C1–C3 扣分、420s 时限）
2. 系统架构与技术框架（Server/Client 双容器、ROS2+CycloneDDS、MuJoCo+DISCOVERSE+3DGS、YOLOv8s、KDL）
3. 核心技术方案（感知定位、逐类几何标定、动作编排状态机、导航避障、安全性设计）
3C. 代码逻辑详解（**答辩重点**：ROS 话题数据流、phase 状态机表、worker 子进程机制、
    五段关键算法逻辑与伪代码、环境变量覆盖机制）
4. 测试结果（8/9 类标定通过 + 典型流程裁判遥测 + 已识别问题如实说明）
5. 系统运行方式（一键启动、常用环境变量、结果判读位置）
6. 工程实践与创新点（5 条可复用方法 + 7 项难点解法对照）
7. 附录（关键文件与数据位置）

**整理过程中发现的两处需注意点**：
- `grasp_geometry.py` 注释里的门禁值（如苹果 0.9188、橙子 0.9713）是**旧版本值**，
  当前公式含 1.12 余量，实际为 0.98 / 1.04。文档中已按当前公式更正并标注来源。
- **橙子门禁 1.04 > 反馈上限 1.0**，等效于"不设抬升门禁"（靠夹空下限 0.85 兜底）。
  不是 bug（判定为 `feedback <= max`，不会误拒），但**该类的抬升门禁实际未生效**，
  后续若要收紧需要重新标定。

��口** —— 两次都是主人从窗口发现异常（太高、撞墙）。

**正确做法**：一次只改一个参数 → 跑一轮 → 读遥测 → 看窗口 → 再改下一个。
��
- 工具修正：run_random5 清场漏 telem 容器（已修）

�� RELIABLE 发布，
BEST_EFFORT 订阅会静默收不到）。

### 踩坑

- `run_random5_autopilot.sh` 清场只清 server/client，**漏了 `${SERVER_NAME}_telem`**
  → 旧遥测容器占名 → 新轮 "Conflict. container name already in use" 启动失败。
  已修 108 行。
- 宿主 Git Bash 被 `export PATH=...` 覆盖会丢 grep/tr/sleep，恢复靠显式设置
  `/c/Windows/System32:/c/Windows:/c/Program Files/Git/bin:/c/Program Files/Git/usr/bin:/usr/bin:/bin`。

轮 dist 0.10-0.165 超时），且逼近段底盘歪斜进货架（第三视角 ~8°）。
默认 SUPERMARKET_CREEP_PURE_PURSUIT=0 弃用；横向 5cm 应由臂侧修正。
对照实验：关掉后苹果一次抓成（842 帧双指接触、抬升 30.9mm，裁判真值）。

## 今日修复清单（全部实测验证）

1. **pure pursuit 弃用**（见上）
2. **朝向容差对齐**：pregrasp 门禁 0.06，抓取相位导航 yaw-tol 同步（否则必败重试）
3. **grip_feedback_min 字段丢失**（_kind_geometry 重建对象漏字段）→ 八类夹空
   检测整体失效 → 修复后夹空正确拦截（0.0806<0.60 被拒），不再空手配送
4. **槽位吸附禁用**：对账吸附版地图 vs 随机真实布局 **1/34（3%）**；
   纯检测聚类召回 89%（40/45）中位 3.5cm、精确 86%。静态布局表在随机局全错
5. **抓前近距复核**（_refine_target_before_ik）：头部对准目标层，2.5s 中位数修正；
   reserved/picking 条目坐标冻结（inventory_map.py），否则复核被检测流冲掉
6. **creep 逐类速度**（creep_speed）：默认 0.15，sanmingzhi/maidong 0.12；23.8s→15.0s
7. **shupian dz +0.030**：低位手被层板抵住（主人目视确认），抬 3cm 解决
8. **配送路线去之字** + 首段载货限速；**预抓取舞步条件化**（省 ~10s/目标）
9. **STOP 线 yaw bug（今日最大发现）**：creep 的 surface_to_center_fwd 修正在收
   meta 时用当时 yaw——比赛流程 meta latched 于 worker 启动前，yaw=0.0 → 修正
   落到 X 轴，停止线浅 3.25cm → 指头平面落后罐身 → 连续夹空。
   修复：_refresh_creep_target 每控制周期用当前 yaw 重算
10. **手眼原生渲染通路**：手眼走 getRgbImg/getDepthImg 进 ROS 话题（不进 GS 批量）；
    阶段开关 handeye_render.flag（executor spawn servo 时写，backoff/close/
    retreat/slide_up 删）。常开原生手眼 RTF 掉到 0.22

## 手眼伺服系统（新建，全链路已跑通到收敛）

- hand_eye_servo_controller.py：HSV 饱和度团块（货架白/灰、夹爪黑被滤），
  取最靠近图像中心者；光轴标定 (u0,v0)=(5.9,65.2)px 用必然成功的苹果标定链
  （STOP_AFTER=creep + 手动探针）实测；fx≈767 cx=480 cy=300 自洽验证
- executor 闭环：横向偏差 >1.5cm（阻尼 0.7、限幅 ±3cm、只修横向 x——z 由标定
  dz 决定，低位全伸展逼近工作空间边界，往真值修 7.4cm 会 deploy 关节卡死）
  → 重部署序列 **backoff_far(30cm) → slide_up(0.87) → deploy → creep → servo 再测**
- 重部署硬超时根因链（逐个排除）：z 修正过大 → RTF 被手眼渲染拖垮 →
  **格内姿态关节插值刮层板**（max_arm_err 卡 0.23）→ 退远+抬手解决
- 实测收敛：+3.3cm → -1.2cm（两轮），横向链路打通

## 当前卡点：伸入深度差 ~6-10cm

伺服帧（debug_data/servo_frame.jpg）显示：罐子横向已在两指正中，但罐顶与
指尖同高——闭爪平面在罐前合拢。深度探测回路已实现：闭爪落空 → creep 停止线
+3cm → 再 creep→close（最多 3 次，depth_probe_count）。+6cm 仍空，第 3 次
到底盘物理极限（y 差 -3.5cm 推不动）。

**最强假设（下一步先试）**：ARM_PREGRASP_Y=2.35/2.40 停车线离罐 0.85m，臂前伸
极限刚好够不到罐身（L2 下探姿态有效前伸更短）→ 试
SUPERMARKET_ARM_PREGRASP_Y=2.55 跑 L2 shupian。注意 deploy 预抓取
= object_y-0.220 会随之加深，需确认 IK 可达（会打印"部署 IK 不可达"）。

**不要用 standoff 做深度控制**：表观尺寸被指头遮挡污染（180px vs 预期 262px）。
用闭爪探测的强反馈（已实现）。

**未解**：近距复核 y 中位数各轮 3.201~3.276 波动（真值 3.243）。货位深 0.5m，
若 randomizer 在槽内随机深度，检测 y 是真实的——那问题就是"够不到"。
核对：docker cp 退出 server 容器的 /tmp/retail_competition_ros2_runtime.xml，
对照 item_03 实际 y（旧拷贝在 debug_data/runtime_seed7.xml，y=3.243）。

## 验证环境速查

- 验证：SUPERMARKET_TASK_COUNT=1 SUPERMARKET_SEED=7 bash baseline/run_random5_autopilot.sh
  （seed7 = 1 单 shupian @ B/L2/C3；真值参照 runtime_seed7.xml，若疑布局变需重拷）
- 裁判遥测：random5_telem/<stamp>_seed7/target_state.jsonl（只含任务目标）
- 苹果标定参照：run_item_grasp.sh KIND=pingguo SLOT=D/L3/C2
  WORLD="0.92 3.243 1.224" STOP_AFTER=creep HOLD_SECONDS=150
  + 探针：echo calib > baseline/debug_data/handeye_render.flag 后
  docker run client 镜像跑 hand_eye_servo_controller.py --execute --confirm servo
- 执行器新增动作：servo / slide_up / backoff_far（WORKER_CFG + ARM_ACTIONS
  插入 servo；servo_redeploy 状态机在 _arm_tick）

## 待办

- [ ] ARM_PREGRASP_Y=2.55 深停车线实验（L2 shupian）
- [ ] 9 类逐一验证伺服抓取（标定链 + random5 各一轮）
- [ ] 全部改动 git 落库（工作区大量未提交，见下）
- [ ] 同类副本重试（alternatives=3-4，失败换下一罐）
- [ ] 多随机种子验证（规则要求）

## 未提交改动清单（git 工作区，务必落库）

executor（近距复核/坐标冻结/伺服闭环/深度探测/slide_up+backoff_far worker/
重试按目标重置/倾角监控/限幅）、grasp_creep_controller（逐类速度/pure pursuit
开关/yaw 安全停止线/creep_extra_dy）、grasp_geometry（creep_speed/
grip_feedback_min 传递修复/shupian dz）、inventory_map（reserved 冻结）、
simulator 补丁（手眼原生渲染+阶段开关）、server_debug_spawn（head GS +
hand-eye native）、run_item_grasp.sh（GS 参数/YAW_TOL/NAV_SPEED）、
run_random5_autopilot.sh（裁判遥测记录器）、hand_eye_servo_controller.py（新）。

---

# 09-12 上午续：j3 横向伺服 + 深度探测（最终状态）

## 伺服修正链的最终形态（全部已提交 3134e54 + 工作区）

- ARM_ACTIONS 顺序：safe_pose → deploy → creep → **servo** → close → lift → ...
- servo worker（抓取面上）：HSV 团块测横向偏差 → **j3 修正**（灵敏度数值 FK
  现算；j1 不是横摆关节，dx 仅 0.128 且污染 y/z——用它修正无效，实测误差不变）
- **u_sign=-1**（+1 方向实测越修越偏：+4.7 → +10.7 → +13.9）
- **单次修正**（修正后画面大变，重测会抓错团块——79px/0.5m 垃圾数据）
- 深度探测：闭爪落空 → creep 停止线 +1.5cm → 再 creep→close（最多 4 次；
  +3cm 步长实测在最优深度两侧跳变：+3cm feedback 0.32 部分接触、+6cm 回 0.08）
- 垂直伺服禁用（SUPERMARKET_SERVO_VERTICAL=0；v0 参考随层高变，L3 标定值
  在 L2 会把手压到格子底 -14.7cm）

## 关键回归修复：obs 字典需要手眼键常驻

handeye_render.flag 门控（标志关闭期间手眼缓冲不填充）导致 obs 管线异常：
**地图只收到 4/45 槽位**（连续两轮"初始地图未找到可用目标"）。
修复：去掉标志门控，手眼每 tick 无条件原生填充（RTF ~0.45 常态可接受）。
修复后 observed=41/45 ✓

## 实测结果汇总（seed7 shupian@B/L2/C3）

| 配置 | 结果 |
|---|---|
| 夜间：stop_dy 0.010 + 伺服(旧j1) | **抓取成功** feedback 0.8091，lift ✓，配送超时 |
| 今晨：+3cm 探测 | 0.32 部分接触（深度方向对了但过冲） |
| 今晨：+6cm 探测 | 0.08 全空（过冲） |
| 今晨：j3 修正 u_sign=+1 | 越修越偏（+4.7→+13.9，方向反） |
| 今晨：u_sign=-1 单次修正 | 探测 1.5/3.0/4.5cm 全空（各轮 y 残差 ±2cm 摆动） |

## 结论与下一步

- 横向：j3 伺服方向修正后待验证（u_sign=-1 的完整抓取轮还没跑完——
  最后一轮跑在 pregrasp 阶段被上下文截断）
- 深度：各轮检测 y 残差 ±2cm 摆动使固定停止线时好时坏；最优深度在
  stop_dy 0.010~0.035 之间某处，且**逐轮不同**（检测 y 残差主导）。
  闭爪探测回路是对的，需要更细的步长/更多次数，或把探测与横向状态耦合
- 配送导航：150s 看门狗 + pkill 僵尸导航已实现；RTF 0.45 下时间紧张，
  考虑 DELIVER_NAV_TIMEOUT 再加大或降低 RTF 压力
- **下一步第一件事**：u_sign=-1 完整跑一轮看横向校准是否落在罐上
  （servo_frame.jpg 诊断帧已实现，绿框=团块，红十字=图像中心）
- 未提交：探测步长 1.5cm、u_sign=-1、max_iterations=1、垂直伺服禁用、
  obs 无条件填充

---

# 09-12 下午：品类指定 + 苹果比赛单（进行中）

## 已落地

1. **GitHub v0.3.0-servo-grasp 已发布**（be5396f，HYXJX2006/supermarket-sorting）
2. **SUPERMARKET_TASKS 品类支持**（random_server_bootstrap.py choose_targets）：
   品类名 token → 该类随机取一罐（task_selection_seed 决定）。此前只认 body 名，
   品类名会 ValueError "unknown SUPERMARKET_TASKS"
3. **近距复核 15cm 上限**：修正幅度超 15cm 弃用（26cm 的"修正"曾把目标拉到
   错误货架导致 IK 不可达）
4. **MAP_TIMEOUT 120→240**（RTF 0.45 下 120s 扫描只完成 E+D，observed 11/45）
5. 批量品类测试脚本 run_kind_tests.sh（SUPERMARKET_TASKS 逐类一单）

## 苹果第一单的失败→修复→重跑（进行中）

- 第一次：布局每轮都在变！（旧 runtime_seed7.xml 作废——苹果实际在 D/L2/C1
  (1.14,3.243,0.886)，不是旧 XML 的 D/L3）。地图 pingguo 聚类是误检
  （z 0.498 的别的东西），近距复核 4 样本跟错 26cm → IK 不可达
- 修复：MAP_TIMEOUT 240 + 复核 15cm 上限 → 重跑中（后台 call_e643e0e5）

## 布局随机性重要发现

runtime XML 每 run 都不同（种子 7 也不稳定）——**每轮诊断必须拷当轮的
/tmp/retail_competition_ros2_runtime.xml**，旧 XML 只能参考不能当真值。

## 待办

- [ ] 苹果单跑完看结果（后台）
- [ ] 9 类逐一（SUPERMARKET_TASKS=品类 TASK_COUNT=1 逐类跑）
- [ ] j3 伺服 u_sign=-1 + 1.5cm 深度探测的组合验证（shupian 上轮探测
  +3cm 时 feedback 0.32 部分接触——深度方向对了，步长改 1.5cm 后未验证）
- [ ] 配送 150s 看门狗 + 倒车脱困的完整验证
- [ ] 全部落库

---

# 09-12 下午续：系统性 3.6cm 横向偏差的定位（未解决，交接）

## 关键实验

TARGET_APPROACH_OFFSET_X 0.12→0.156（底盘停靠 +3.6cm 补偿）后，伺服仍测得
+3.0/+4.8/+4.7cm——**偏差不在底盘停靠位，在手臂链路**（deploy 后关节跟随
误差 max_arm_err 0.19-0.29 rad，末端即 4-8cm 级）。

且 j3 修正的实际效果与模型不符：修正 -0.0375/-0.0603/-0.0586（按 -0.4 m/rad），
误差仍 +3.0→+4.8→+4.7 不降——j3 的真实灵敏度/方向未知，或者团块测量本身
不可靠（红罐 vs 其他红色物体）。

## 下次会话的诊断路径（按序，别跳）

1. **量化手臂真实跟踪误差**：机器人停在抓取位、手臂张开后，用头相机
   RGB-D 测夹爪指尖的实际世界坐标，与 FK 对比——直接量化"指令 vs 实际"
   的差。这一步定调：是手臂跟不上（deploy 插值/收敛问题）还是 FK 模型错
2. **j3 修正受控实验**：固定其他一切，j3 单独 +0.05/-0.05，头相机测 ee
   实际移动方向和幅度——确定 j3 的真实灵敏度和符号
3. 确认后再修：优先考虑 deploy 收敛门（deploy_position_tolerance 0.065
   太松——6.5cm 误差也算"到位"！收紧到 0.02-0.03，让 deploy 多等一会）
4. 苹果单在 D/L2 (1.14,3.243,0.886) 的完整验证（MAP_TIMEOUT 240 已生效）
5. 9 类逐一：SUPERMARKET_TASKS=品类 TASK_COUNT=1（bootstrap 已支持品类名）

## 环境备忘

- 验证命令：SUPERMARKET_TASKS=kouxiangtang SUPERMARKET_TASK_COUNT=1
  SUPERMARKET_SEED=7 bash baseline/run_random5_autopilot.sh
- viewer_stream 容器（8090 三视角）已起；诊断帧 debug_data/servo_frame.jpg
- 薯片 creep_stop_dy=0.010 已固化且 random5 脚本默认值已同步 0.010
- 两次提交：3134e54、0a6bc74、be5396f（v0.3.0-servo-grasp 已推 GitHub）
- 未提交：TARGET_APPROACH_OFFSET_X 0.156、近距复核 15cm 上限、MAP_TIMEOUT
  240、探测步长 1.5cm、j3 阻尼 0.5、垂直伺服禁用

---

# 09-12 下午终版：橙子抓取成功 + 配送底盘冻结（新拦路虎）

## 橙子单：抓取 ✅

v6 权重（混淆微调后）+ 伺服校准 → 闭爪+抬升完成。v6 分类验证：
kouxiangtang 0.96 / shupian 0.92 / chengzi 0.88 / sanmingzhi 0.86——
chengzi/kouxiangtang 混淆修复。

## 新拦路虎：配送段底盘冻结

配送段从一开始就以 ~1% 速度爬行后完全冻结（pos/heading 10s 零变化、
front 0.72 畅通、导航指令正常）。卡死倒车触发了但车依旧不动——
**底盘在配送段不执行 cmd_vel**。已提交卡死检测+倒车脱困（会触发但不够）。

**下次会话第一步（诊断）**：手动直接向 /cmd_vel 发 Twist（docker exec 进
client 容器或用 ros2 topic pub），看底盘动不动：
- 动 → 导航 worker 发的是零速 → 查 navigator 内部状态
- 不动 → 查 chassis 订阅/sim 的 cmd_vel_callback（重点：配送段开始时
  是否有异常——对比抓取前 pickup-transit 段底盘是正常的）

注意：抓取前的 pickup-transit 段底盘也是极慢的（turn-in-place 日志
pos 长时间不动）——**底盘慢/冻可能贯穿全场**，RTF 0.45 解释一部分，
剩下的大头待查。

## 已提交（34b9bac）
- run_kind_tests.sh（批量品类测试）
- 橙子/口香糖/脉动分类修复（v6 权重已部署为默认）
- 类别错配守卫、配送卡死倒车、深度探测 1.5cm 步长

## 下一会话清单
1. /cmd_vel 直接诊断（上面说的二分法）
2. 修好后：chengzi/kele 完整单（抓+送+放+得分）
3. 9 类批量（run_kind_tests.sh，TaskCount=2 双单模式）
4. RTF 恢复正常后再全流程压测（比赛时限 240s 的真实评估）

## 21:00 安装 DeepSeek Harness CLI（dsh）

- 目标：全局安装 @deepseek-ai/dsh@0.1.5-rc.1，实得版本一致，落在 C:/Users/hyx/AppData/Roaming/npm。
- 规模：518 个包 / 全局 node_modules 641 MB，安装耗时 12 分钟。
- 网络：官方源 registry.npmjs.org 直连实测仅 76 KB/s（30 秒差分法测得），切 --registry=https://registry.npmmirror.com 后达 2.5 MB/s，约 33 倍；镜像 dist-tags 与官方完全一致。仅本次生效，未改全局 npm 配置。
- npm allow-scripts 拦截了 5 个包的 install script（koffi / node-pty / dsh-subprocess-local / @google/genai / protobufjs）。实测确认不构成缺口：koffi 可 require（v3.2.1）、node-pty 自带 prebuilds/win32-x64、ensure-spawn-helper.mjs 只做 Unix chmod 755 在 Windows 上无实质作用。教训：按扩展名找不到 .node 就判缺失是错的，必须 require 实测。
- 验收证据（非退出码）：dsh --version 返回 0.1.5-rc.1；dsh web 监听 3080；UI 返回 HTTP 200 / text/html / 27660 字节 / title "DeepSeek Harness"，并加载 /assets/index-*.js 与 /plugins 模块。
- 鉴权机制：访问 /?token=<32位> 返回 303 并下发 Cookie dsh-auth-<hash>，之后靠 Cookie 放行；裸访问 / 返回 401。node fetch 不自动管理 Cookie，跟随重定向会丢凭证从而误判 401。
- 隔离沙箱目录：C:/Users/hyx/dsh-sandbox，未触碰揭榜挂帅项目文件。

## 21:28 dsh web EADDRINUSE 处理

- 主人自行运行 dsh web 报 EADDRINUSE 127.0.0.1:3080，原因是上一轮我启动的后台实例仍占用该端口。
- 已停止后台实例，实测端口 free、HTTP probe = ECONNREFUSED，确认释放。
- 约定：dsh web 由主人自己的终端启动，不长期挂在我的后台会话里，避免端口占用与会话回收后不可控。

---

# 09-12 夜：比赛版全流程跑三明治 + 三处修复（22:30~23:15）

## 1. 导航起步"左走一点"已修复并实测（commit 5ccaceb）

`_goto_observe` 走廊分支原来写死 `bypass_x=1.70`，起点 x=1.92 只有 22cm 横移却要先
左转 90°（max-angular 0.45 → 3.5s 起），转向切直行时前进速度非零会沿弧线漂出
"左走一点"。改为 `bypass_x = start_x`（当前 odom x）后：

| 指标 | 改前 | 改后 |
|---|---|---|
| 起步第一次原地转向 | 左转 90°，5s | 无 |
| 起步横向漂移 | 1.92 → 1.85 | 0cm（x 恒 1.92） |
| 起步航向误差 | 0.05~0.07 rad | 0.00 rad |
| 出发区→第一个观察位 | ~51s 仿真 | ~30s |

顺带：`run_random5_autopilot.sh` 清场漏 `${SERVER_NAME}_telem` → container name
already in use 掐死脚本（"已修"其实从没落地），已补。

## 2. 三明治夹得住（夹具实测，裁判真值）

`run_item_grasp.sh KIND=sanmingzhi SLOT=E/L1/C1 WORLD="1.585 3.243 0.5484"` →
deploy/creep/close/lift 全 success；闭爪 feedback 0.8116（空夹 0.99999，苹果 0.8091）；
真值 **z 0.5484→0.59195（+4.36cm）、双指接触 true/true、xy_shift 0.43cm、tilt 0.91°**。
→ 物理抓取链路对三明治是好的，问题在视觉/守卫，不在手上。

## 3. 三处新修复（本轮 commit）

- **候选置信度门槛**：`_target_order` 原来只用置信度排序、从不设门槛，导致
  `D/L2/C2` 被一条 conf=0.15（检测器下限）的 sanmingzhi 弱检写进地图并因路线最短被预订，
  开过去近距看到的是 heweidao 0.963 → 守卫否决 → 整轮 0 分。
  新增 `MIN_CANDIDATE_CONFIDENCE=0.50`（`SUPERMARKET_MIN_CANDIDATE_CONFIDENCE`）。
- **类别错配不再一票判死**：`_refine_target_before_ik` 的错配分支改为拉黑该槽位
  （`rejected_slots` + `map.mark(slot,"invalid")`）、重新 `_target_order()` 换同 kind 候选，
  用尽（上限 3）才 `_fail_current`。
- **预抓取线小位移死循环**：机器人已在预抓取线容差内时，executor 仍发"只前进 3~5cm"的
  pickup-approach 目标 → remaining_along 变负 → `_pickup_reverse_latched` → heading_err 在
  ±0.35 来回穿越、原地打转 240s 超时（实测 C/L2/C3 目标耗掉 ~100s 仿真时间，重试同样结局）。
  修法：贴线时把 `pregrasp_y` 钉在当前 y（只走回正）。

## 4. 踩坑：进程托管

用 pwsh 后台任务托管 `wsl.exe ... run_random5_autopilot.sh` 时，任务被中断会连带把
client(143)/server(255)/viewer(255) 一起带走。改用 `baseline/launch_comp.sh`
（WSL 内 setsid + 日志落盘 `/opt/jbgz/run/comp_<kind>.log`）后稳定。
另：`/opt/jbgz/baseline` 是 WSL 原生副本（走 9P 太慢），改完代码要同步过去。